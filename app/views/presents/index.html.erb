<div class="container marketing">
  <div class="row">
<h1>Seznam darů</h1>

<table>
  <tr>
    <th>Název</th>
    <th>Obsazeno</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @presents.each do |present| %>
  <tr>
    <td><%= present.name %></td>
    <td><%= present.pole %></td>
    <td><%= link_to 'Shlédnout', present %></td>
    <td><%= link_to 'Obsadit', edit_present_path(present) %></td>
    <td><%= link_to 'smazat', present, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nový dar', new_present_path %>
 </div>
    </div>